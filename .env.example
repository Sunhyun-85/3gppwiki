# Never commit the populated .env file.
RAN2WIKI_CONFIG=config.yaml
RAN2WIKI_WEB_PORT=3000
OPEN_WEBUI_PORT=3001
# Only used by the optional future `llm` profile.
WEBUI_SECRET_KEY=
# Obtain after `tailscale up` with: tailscale ip -4
TAILSCALE_BIND_ADDRESS=127.0.0.1
OPEN_WEBUI_IMAGE_TAG=v0.6.42
